{
  "name": "email-tracker-client",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "vite": "^5.3.1",
    "vitest": "^4.1.10"
  }
}
